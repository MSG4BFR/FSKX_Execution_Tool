# Secrets — never commit your real API key.
.env
.env.*
!.env.example

# Local model library / repository downloads (runtime data, not source).
fskx_models/

# Python
app/__pycache__/
*.pyc
.DS_Store
